#include<stdio.h>
int main(){
	int n,c=0,i;
	printf("enter a num");
	scanf("%d",&n);
	for(i=2;i<=n;i++){
		if(n%i==0)
		c++;
		
	}
	if(c==1)
	printf("it is prime number");
	else
	printf("it is not prime num");
	return 0;
	
}
